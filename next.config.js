/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      // All images now served locally from /public directory
    ],
  },
}

module.exports = nextConfig

